import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.notifyassetmain',
  appName: 'notify-asset-main',
  webDir: 'dist'
};

export default config;
